#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *rev_string - a function that reverses a string.
 *@s: value to be evaluate.
 *Return: not.
 */
void rev_string(char *s)
{
int i;
i = 0;
while (s[i]);
}
