#include "main.h"
/**
 * add - checks for lower case character
 * @a: the character to check for
 * @b: the character to check for
 * Description: adds two integers and returns the result.
 * Return: 1 if c is lower case. 0 otherwise
 **/
int add(int a, int b)
{
return (a + b);
}
