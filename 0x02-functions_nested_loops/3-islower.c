#include "main.h"
/**
 * _islower - checks for lower case letters
 * @alphabet: the character to check for
 * Description: checks for lower case letters.
 * Return: 1 if alphabet is lower case. 0 otherwise
 **/
int _islower(int alphabet)
{
return (alphabet >= 'a' && alphabet <= 'z');
}
