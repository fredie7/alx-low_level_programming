#include "main.h"
/**
 *main - first point
 * Return: always 0
 */
void print_alphabet()
{
char alphabet;
for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
{
_putchar(alphabet);
}
_putchar('\n');
return (0);
}
