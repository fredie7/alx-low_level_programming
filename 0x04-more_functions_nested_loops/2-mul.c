#include "main.h"
#include <ctype.h>
/**
 * int mul multiplies 2 integers
 * @a: the character multiplies the second character in the funcion parameter
 * @b: the character  multiplies the first character in the funcion parameter
 * Description: multiplies 2 integers
 * Return: the multiplication of 2 integers
 **/
int mul(int a, int b)
{
return (a * b);
}
