/**
 * main - first point
 * Return: always 0
 */
#include <stdlib.h>
#include <time.h>
#include <stdio.h>
int main(void)
{
int num;
while (num < 16)
{
printf("%d", num);
num++;
}
putchar('\n');
return (0);
}
