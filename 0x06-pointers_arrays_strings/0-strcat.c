#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *char *_strcat - a function that concatenates two strings.
g*@dest: first value to be evaluated.
 *@src: second value to be evaluted
 *Return: not.
 */
char *_strcat(char *dest, char *src)
{
return (strcat(dest, src));
}
